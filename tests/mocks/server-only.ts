// Test-only stub for the `server-only` package, which otherwise throws when
// imported outside a Next.js server build. Safe to no-op under Vitest since
// tests run in a plain Node environment, not a client bundle.
export {};
